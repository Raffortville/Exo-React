import React from 'react'

const NavFieldPage = (props) => {

    return(

        <div className="currentFieldMenu">
            <span>{props.title}</span>
        </div>
    )
}


export default NavFieldPage